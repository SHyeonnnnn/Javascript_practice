const num=2.1234;

const maxNum=Math.max(77,5,4,9,30);
console.log(maxNum);
const minNum=Math.min(10,5,8,3,1);
console.log(minNum);
const roundNum=Math.round(num); // 소수점 첫째 자리에서 반올림
console.log(roundNum);
const floorNum=Math.floor(num); // 소수점 첫째 자리에서 내림
console.log(floorNum);
const ceilNum=Math.ceil(num); // 소수점 첫째 자리에서 무조건 올림
console.log(ceilNum);
const rndNum=Math.random(); // 0~1사이의 난수 발생 
console.log(rndNum);
const piNum=Math.PI;
console.log(piNum);

const rn=Math.floor(Math.random()*11); //0부터 10까지 난수를 발생
console.log(rn);
const rn1=Math.floor(Math.random()*31)+120; //120부터 150까지 난수를 발생
console.log(rn1);